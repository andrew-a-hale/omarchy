#!/bin/bash

# browser=$(xdg-settings get default-web-browser)
# e=$(sed -n 's/^Exec=\([^ ]*\).*/\1/p' {~/.local,~/.nix-profile,/usr}/share/applications/$browser 2>/dev/null | head -1)
# exec setsid uwsm app -- $e --app="$1" "${@:2}"
exec setsid uwsm app -- chromium --app="$1" --profile-directory="$2" "${@:3}"
